# Basic .htaccess for PHP contact form on typical shared hosting
# Ensure PHP mail() works and set reasonable defaults.

# Allow PHP to execute
AddHandler application/x-httpd-php .php

# Optional: set PHP mail sendmail_path if your host requires it
# php_value mail.force_extra_parameters "-t harishahi592@gmail.com"

# Disable directory listing (security)
Options -Indexes

# Restrict access to contact.php to POST only (optional)
<Files "contact.php">
  <LimitExcept POST>
    Require all denied
  </LimitExcept>
</Files>
